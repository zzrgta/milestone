<table class="table is-bordered is-striped is-hoverable is-fullwidth">
    

    <head>
        <meta charset="utf-8">
        <title>Lab #2</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
        <style>
            #logo {
        float: right;
    }
            #p4 {
        clear: both;
    }
    
    .flex-item {
        background-color:skyblue;
        height: 250px;
        width: 150px;
        line-height: 50px;
        text-align: start;
        margin: 5px;
        flex-grow: 1
    }
    
    .flex-container {
        margin: 0px;
        padding: 0px;
        font-size: 100%;
        display: flex;
        justify-content: space-around;
        flex-wrap: wrap;
    }
    
        </style>
    </head>

    <section class="section">

        <div class="container">
            <div class="control">
                  <a id="Home" href="http://localhost:1337/">Home</a>
                  <a id="Create" href="http://localhost:1337/restaurant/create">Create</a>
                  <a id="Search" href="http://localhost:1337/restaurant/search">Search</a>
                  <a id="Admin" href="http://localhost:1337/restaurant/admin ">Admin</a>
            </div>
        </div>

        <div class="container">
            <h1 class="title">
              Q-Pon
            </h1>
            <p class="subtitle">
              Redeem your coins for dining <strong>Coupon</strong>!
            </p>
        </div>
    
    </section>

    <% restaurantsK.forEach( function(restaurant) { %>
        <section>
            <p class="control"><%= restaurant.region%></p>
            <div class="flex-container">
                <img src="<%= restaurant.image %>"/>
                <a class="flex-item" href="http://localhost:1337/restaurant/read/<%= restaurant.id %>"><%= restaurant.Rname %> <br><%= restaurant.title %></a>
            </div>
        </section>
    <% }); %>

    <% restaurantsH.forEach( function(restaurant) { %>
        <section>
            <p class="control"><%= restaurant.region%></p>
            <div class="flex-container">
                <img src="<%= restaurant.image %>"/>
                <a class="flex-item" href="http://localhost:1337/restaurant/read/<%= restaurant.id %>"><%= restaurant.Rname %> <br><%= restaurant.title %></a>
            </div>
        </section>
    <% }); %>

    <% restaurantsN.forEach( function(restaurant) { %>
        <section>
            <p class="control"><%= restaurant.region%></p>
            <div class="flex-container">
                <img src="<%= restaurant.image %>"/>
                <a class="flex-item" href="http://localhost:1337/restaurant/read/<%= restaurant.id %>"><%= restaurant.Rname %> <br><%= restaurant.title %></a>
            </div>
        </section>
    <% }); %>

    <script>
       
    </script>
</table>